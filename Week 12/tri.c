#include <stdio.h>
#include <stdlib.h>
#include <assert.h>

int tri(int n) {
	return 0;
}


int main(int argc, char* argv[]) {
	printf("tri(%d) = %d",0,tri(0));
	printf("tri(%d) = %d",1,tri(1));
	printf("tri(%d) = %d",5,tri(5));
	return EXIT_SUCCESS;
}